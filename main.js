const {generateSequences, maxSubArrayNonConsecutive} = require("./triple_step");

generateSequences([-2, 1, 3, -4, 5]);
generateSequences([-2, 1, 3, -4, 5, 2]);
maxSubArrayNonConsecutive([-2, 1, 3, -4, 5]);

